# Backup de planes (Cursor)

Este directorio contiene copias de los planes (`*.plan.md`) creados por Cursor/AI.

## Cómo actualizar el backup

Ejecuta:

```bash
./scripts/backup_plans.sh
```

Esto copia los archivos desde `~/.cursor/plans/*.plan.md` a `docs/plans/`.

